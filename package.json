{
  "name": "i-home",
  "version": "1.0.0",
  "private": true,
  "description": "A calm, modular home dashboard for Obsidian.",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && node build.mjs",
    "check": "tsc --noEmit",
    "test": "node build.mjs --tests && node test/store.test.cjs && node test/model.test.cjs && node test/reviews.test.cjs"
  },
  "devDependencies": {
    "obsidian": "^1.8.7",
    "esbuild": "^0.25.5",
    "typescript": "^5.8.3",
    "@types/node": "^22.15.17"
  }
}
